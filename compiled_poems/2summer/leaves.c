#include <stdio.h>
#include <unistd.h>

#define thisway "////////////////////////\n"
#define thatway "\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\n"

#define fierce(x, y) if (x) { printf("%s", y); x++; }
#define calm(x, y) if (!x) { printf("%s", y); --x; }
#define the (;
#define afternoon ;) {

#define soundly 1

#define blows = 0;

#define summer int main() { int

int main() { int
   wind = 0;

   for the afternoon
      if (wind) {
         printf("%s", thisway);
         wind++;
      }
      else if (!wind) {
         printf("%s", thatway);
         --wind;
      }
      sleep(soundly);
   }

}
