#include <stdio.h>
#define flowers fprintf(stdout,
#define everywhere "%s\n"
#define day );
#define One summer today(const char * to) {
#define a
#define stray(x) return (x); }
#define Two int main() {

typedef enum {
    butterfly
} summer;

// ===================================

One
    summer afternoon;
    flowers everywhere, to--day
    a stray (butterfly)

Two
    summer afternoon;
    today("there were no flowers...");
    return butterfly;
}
